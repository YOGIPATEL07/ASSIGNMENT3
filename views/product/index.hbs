<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f9f9f9; /* Light background for a clean look */
            font-family: 'Arial', sans-serif;
        }
        .container {
            max-width: 1200px; /* Larger container for better layout */
        }
        .header-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #007bff;
            margin-bottom: 1.5rem;
        }
        .card {
            border: 1px solid #e0e0e0; /* Light gray border for subtle contrast */
            border-radius: 15px;
            background-color: #ffffff; /* White background for cards */
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
            margin-bottom: 1.5rem; /* Spacing between cards */
        }
        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }
        .card-img-top {
            border-radius: 15px 15px 0 0;
            object-fit: cover;
            height: 250px; /* Increased height for better image display */
            width: 100%; /* Ensures image covers the width of the card */
        }
        .card-body {
            padding: 1.5rem;
        }
        .card-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: #333;
        }
        .card-text {
            color: #6c757d;
            margin-bottom: 1rem;
        }
        .btn-group {
            margin-top: 1rem;
            display: flex;
            flex-wrap: wrap; /* Allow buttons to wrap if they overflow */
            gap: 10px; /* Space between buttons */
        }
        .btn-group .btn {
            flex: 1 1 auto; /* Allow buttons to grow and shrink as needed */
        }
        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
        }
        .btn-success:hover {
            background-color: #218838;
            border-color: #1e7e34;
        }
        .btn-warning {
            background-color: #ffc107;
            border-color: #ffc107;
        }
        .btn-warning:hover {
            background-color: #e0a800;
            border-color: #d39e00;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-danger:hover {
            background-color: #c82333;
            border-color: #bd2130;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <h2 class="header-title">Products</h2>
        {{#if user}}
        <a href="/products/add" class="btn btn-success mb-3">Add Product</a>
        {{/if}}
        <div class="row">
            {{#each products}}
            <div class="col-md-4 mb-4">
                <div class="card">
                    {{#each imageFiles}}
                    <img src="/images/{{this}}" class="card-img-top" alt="{{../name}}">
                    {{/each}}
                    <div class="card-body">
                        <h5 class="card-title">{{name}}</h5>
                        <p class="card-text">{{description}}</p>
                        <p class="card-text">Price: ${{price}}</p>
                        <p class="card-text">Stock: {{stock}}</p>
                        {{#if ../user}}
                        <div class="btn-group" role="group" aria-label="Product Actions">
                            <a href="/products/edit/{{_id}}" class="btn btn-warning btn-sm">Edit</a>
                            <form action="/products/delete/{{_id}}" method="post" class="d-inline">
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                            <form action="/products/add-to-cart/{{_id}}" method="post" class="d-inline">
                                <button type="submit" class="btn btn-primary btn-sm">Add to Cart</button>
                            </form>
                        </div>
                        {{/if}}
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
   
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
</body>
</html>
