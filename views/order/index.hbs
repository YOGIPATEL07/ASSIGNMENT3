<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light gray background */
            font-family: 'Arial', sans-serif;
        }
        .container {
            max-width: 1200px; /* Larger container for more space */
        }
        .header-title {
            font-size: 2rem;
            font-weight: 700;
            color: #007bff;
            margin-bottom: 1.5rem;
        }
        .table {
            background-color: #ffffff; /* White background for table */
            border-radius: 0.5rem; /* Rounded corners */
            box-shadow: 0 0 0 0.1rem rgba(0, 0, 0, 0.1); /* Light shadow */
        }
        .table th, .table td {
            vertical-align: middle;
        }
        .table thead th {
            background-color: #007bff;
            color: #ffffff;
            text-align: center;
            font-weight: 600;
        }
        .table tbody tr:hover {
            background-color: #f1f1f1; /* Light gray for hover effect */
        }
        .table tbody td {
            text-align: center;
            font-size: 0.9rem;
        }
        .btn {
            font-weight: 600;
        }
        .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-danger:hover {
            background-color: #c82333;
            border-color: #bd2130;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .btn-sm {
            padding: 0.375rem 0.75rem;
            font-size: 0.875rem;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <h2 class="header-title">Orders</h2>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Order ID</th>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Total Price</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {{#each orders}}
                <tr>
                    <td>{{this._id}}</td>
                    <td>
                        {{#each this.items}}
                        {{this.product.name}}<br>
                        {{/each}}
                    </td>
                    <td>
                        {{#each this.items}}
                        {{this.quantity}}<br>
                        {{/each}}
                    </td>
                    <td>${{this.total}}</td>
                    <td>
                        <form action="/orders/delete/{{this._id}}" method="post" class="d-inline">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                        <a href="/orders/invoice/{{this._id}}" class="btn btn-primary btn-sm">Download Invoice</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    <script src="/javascripts/main.js"></script>
</body>
</html>
